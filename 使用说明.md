# 个人网站使用说明书

您的个人网站已经搭建完成！所有文件都整理在 `docs/` 文件夹中。这份文档将指导您如何查看、修改和维护您的网站。

## 1. 快速开始

-   **网站位置**: `e:\personal_web\docs\`
-   **如何预览**: 双击打开 `docs` 文件夹下的 `index.html` 文件，即可在浏览器中查看您的网站。
-   **线上地址**: `https://edgclearlove7777777.github.io/personal-website/`

## 2. 网站结构说明

文件夹及文件用途如下：

-   **`index.html`**: 网站的主页文件（骨架）。
-   **`css/style.css`**: 样式文件，负责网站的外观（颜色、字体、布局）。
-   **`js/main.js`**: 逻辑文件，负责导航栏切换和内容渲染。
-   **`js/translations.js`**: **【核心内容文件】**。所有的文字内容（中英文）、图片路径、新闻列表都在这里修改。
-   **`assets/`**: 存放图片资源（头像、项目图片、证书等）。
-   **`extracted_text/`**: 存放您的简历文本和PDF文件。

## 3. 如何修改网站内容（最重要）

为了方便管理中英文双语，我将所有内容都提取到了 `js/translations.js` 文件中。您只需要修改这个文件即可更新网站内容。

### 修改文字内容
1.  用代码编辑器（如 VS Code）打开 `docs/js/translations.js`。
2.  您会看到两个主要部分：
    -   `en: { ... }`: 英文内容
    -   `cn: { ... }`: 中文内容
3.  找到对应的字段进行修改，例如修改邮箱或简介：
    ```javascript
    email_label: "Email",  // 英文版标签
    about_text: "在这里修改您的个人简介...", // 支持 HTML 标签如 <br> 换行
    ```

### 添加/修改新闻和证书
在 `js/translations.js` 中找到 `news_items` 数组。
-   **格式**:
    ```javascript
    {
        date: "2025.06",
        text: "新闻描述文字",
        images: ["assets/award1.png", "assets/award2.jpg"] // 可选：证书图片路径
    }
    ```
-   **图片**: 将您的证书图片放入 `docs/assets/` 文件夹，然后在这里填写正确的文件名。

### 添加/修改科研项目
在 `js/translations.js` 中找到 `projects_items` 数组。
-   **格式**:
    ```javascript
    {
        title: "项目名称",
        period: "时间段",
        desc: "项目描述...",
        tags: ["标签1", "标签2"],
        image: "assets/project_image.jpg" // 项目封面图路径
    }
    ```

## 4. 如何更换图片

请将您的图片文件直接放入 `docs/assets/` 文件夹中，并确保文件名与代码中引用的一致，或者修改代码中的文件名。

-   **头像**: 默认引用名为 `assets/profile.jpg`。您可以直接替换该文件，或者放入新照片并在 `docs/index.html` 第31行修改路径。
-   **学校Logo**:
    -   NUS Logo: `assets/logo_nus.png`
    -   电子科大 Logo: `assets/logo_uestc.png`
-   **如果图片不显示**: 网站会自动隐藏加载失败的图片，保持页面整洁。

## 5. 如何更新简历 (CV)

当您有新的简历版本时，按以下步骤更新：

1.  **准备文件**: 准备好您的英文简历 (Word/PDF) 和中文简历 (PDF)。
2.  **重命名**: 将文件命名为：
    -   英文: `Fengxiaoxiao_Li_CV.docx` (或 `.pdf`)
    -   中文: `Fengxiaoxiao_Li_CV_CN.pdf`
3.  **替换**: 将这两个文件复制到 `docs/assets/` 文件夹中，覆盖（替换）原有的文件。
4.  **注意**: 如果您更改了文件后缀（例如 `.docx` 变成了 `.pdf`），请务必去 `docs/index.html` 中修改对应的下载链接代码。

## 6. 如何添加新的论文 (Publications)

在 `js/translations.js` 中找到 `publications_list` 数组。
这是一个简单的文字列表，您可以直接复制一行并修改内容。支持 HTML 格式（例如使用 `<strong>` 加粗作者名，`<em>` 斜体会议名）。

```javascript
publications_list: [
    "<strong>Fengxiaoxiao Li*</strong>, Author B, ... \"Title.\" <em>Conference/Journal</em>, Year."
]
```

## 7. 进阶：修改网站主题颜色

如果您想改变网站的主色调（目前是深蓝色），请修改 `docs/css/style.css`。

1.  打开 `docs/css/style.css`。
2.  找到最顶部的 `:root` 部分：
    ```css
    :root {
        --primary-color: #003D7C; /* 在这里修改十六进制颜色代码 */
        /* ... */
    }
    ```
3.  修改 `#003D7C` 为您喜欢的颜色（例如黑色 `#000000` 或深红 `#800000`），保存后页面所有相关颜色都会自动更新。

## 8. 网站发布与更新 (Git 操作指南)

当您在本地修改了内容（比如加了新项目或更新了简历）后，需要推送到 GitHub 才能在线上看到变化。

**步骤：**
1.  **打开终端**: 在 `e:\personal_web` 文件夹下右键打开 "Open in Terminal" (或 Git Bash)。
2.  **输入以下三条命令**（每输一行按回车）：
    ```bash
    git add .
    git commit -m "update website content"
    git push
    ```
3.  **等待**: 命令执行完毕后，等待约 1-3 分钟，刷新您的线上网址即可看到更新。

---
**常见问题**:
- 如果 `git push` 报错，通常是因为线上有更新而本地没有同步。此时先运行 `git pull`，然后再运行 `git push`。
